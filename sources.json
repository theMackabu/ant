{
  "engine": {
    "patterns": ["src/*.c", "src/esm/*.c", "src/cli/*.c", "src/modules/*.c"],
    "exclude": ["src/main.c"]
  },
  "library": {
    "patterns": ["src/*.c", "src/esm/*.c", "src/modules/*.c"],
    "exclude": ["src/main.c"]
  },
  "core": {
    "patterns": ["src/core/**/*.ts"],
    "exclude": ["src/core/index.ts"]
  }
}
