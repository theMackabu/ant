# Shared build configuration
# Variables expected from parent:
#   src_root    - path to project root (for finding sources)
#   vendor_dir  - name of vendor/subproject directory
#   is_static   - whether to link dependencies statically

subdir('deps')
subdir('version')
subdir('snapshot')
